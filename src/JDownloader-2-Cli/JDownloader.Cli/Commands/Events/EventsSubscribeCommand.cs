using JDownloader.Cli.Runtime; using JDownloader.Cli.Transport;
namespace JDownloader.Cli.Commands.Events; public sealed class EventsSubscribeCommand : EventsCommandBase { public EventsSubscribeCommand(IProfileResolver a, IOutputRenderer b, IDiagnosticLogger c, IMyJdTransport d, IConfirmationGuard e) : base(a, b, c, d, e) { } protected override string Operation => "events.subscribe"; protected override string Endpoint => "/events/subscribe"; protected override bool Destructive => true; }
